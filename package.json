{
  "name": "m-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "22.x" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node --env-file-if-exists=.env backend/local.mjs",
    "test": "node --test tests/backend.test.mjs",
    "check": "node scripts/build.mjs && node scripts/check.mjs"
  }
}
